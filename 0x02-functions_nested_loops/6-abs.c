#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes absolute value
 * abs - absolute value
 * @n: number to print
 * Return: absolute value
 */

int _abs(int n)
{
	int abs_n = abs(n);

	return (abs_n);
}

